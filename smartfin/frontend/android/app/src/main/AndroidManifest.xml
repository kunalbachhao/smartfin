<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        RECEIVE_SMS (dangerous — runtime grant required on Android 6+)
        Allows the app to be notified when a new SMS arrives via a
        BroadcastReceiver. Used to intercept OTP messages and auto-fill
        the verification code without the user having to switch apps.
        Classification and storage happen entirely on-device.
    -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
        READ_SMS (dangerous — runtime grant required on Android 6+)
        Allows the app to read the SMS inbox for historical transaction
        back-fill via SmsSyncService. Required by the sms_advanced package
        to query the device inbox. Classification and storage happen
        entirely on-device.
    -->
    <uses-permission android:name="android.permission.READ_SMS" />

    <!--
        FOREGROUND_SERVICE — required to call startForeground() on API 28+.
        Normal protection level; granted automatically at install time.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
        FOREGROUND_SERVICE_DATA_SYNC — required on API 34+ to declare the
        foreground service type used for data processing (SMS monitoring).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <application
        android:label="smartfin"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            SmsReceiver — explicit broadcast receiver for incoming SMS.
            Required on Android 8+ (API 26+) because implicit broadcasts
            are no longer delivered to manifest-registered receivers.
            Declaring it explicitly here ensures RECEIVE_SMS works across
            all supported API levels. The actual receiver class will be
            implemented when the SMS auto-fill logic is added.
        -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!--
            SmsForegroundService — hosts a headless FlutterEngine that keeps
            the Dart isolate alive when the app is backgrounded or killed.
            foregroundServiceType="dataSync" is required on API 34+ for
            services that process data in the background.
        -->
        <service
            android:name=".SmsForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
